using System;
namespace Usno
{
    class Nerax
    {
        static void Main()
        {
            double res = (0.3 * Math.Pow(5, 2) - 15) / (3.5 + Math.Pow(2, 2));
            Console.WriteLine(res);
        }
    }
}